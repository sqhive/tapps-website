/**
 * Editor component
 */

import React, {Component} from 'react';

class EditorCompiler
  extends Component {

  render() {
    return (
      <FlatButton
        label = "Ok"
        primary = { true }
      />
    );
  }
}

export default EditorCompiler;
